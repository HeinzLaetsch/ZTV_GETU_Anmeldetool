apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ztv-anmeldetool.fullname" . }}-test-connection"
  labels:
    {{- include "ztv-anmeldetool.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ztv-anmeldetool.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

{{- $fullName := print (include "ztv-anmeldetool.fullname" .) "-" "backend" -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ print $fullName "-service" | quote }}
spec:
  selector:
    app: {{ $fullName | quote }}
  ports:
    - protocol: TCP
      port: {{ .Values.backend.port }}
      targetPort: {{ .Values.backend.port }}

